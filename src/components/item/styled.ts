import styled from '@emotion/styled';

export const Wrapper = styled.div`
  text-transform: capitalize;
  font-size: 18px;

  & a {
    color: #000;

    &:visited {
      color: #960505;
    }
  }
`;
